# Odaily Home

Odaily Home is an Obsidian plugin that replaces empty new tabs with a lightweight Odaily default page.

## Development

```bash
npm install
npm run build
```

For local testing, copy or symlink this folder into:

```text
<vault>/.obsidian/plugins/odaily-home
```

Then enable the plugin in Obsidian.
